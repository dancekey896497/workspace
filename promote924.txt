下面这份可以直接复制给另一台 PC 上的 Agent。任务是**实际克隆源码、固定提交、核对调用关系和准确行号，最后生成能离线打开的 HTML 报告**，而不是把我们前面的解释重新排版。

其中专门要求分别核查 **LMCache-Ascend 的补丁式适配、`AscendIPCWrapper` 对 TorchNPU Storage 的调用，以及 LMCache 的 `DeviceSpec` 接口**，避免把不同版本的实现拼成一条实际上不存在的调用链。:chatgpt-content-reference{index="0"}

````text
你现在负责一项“源码核查＋分层教学＋HTML 报告”的任务。请直接执行，不要只给方案或目录草稿。

这台 PC 是源码分析环境，不是运行 NPU 服务的服务器。你的任务是克隆相关公开源码、固定具体提交、查找准确的代码位置、核查层级与版本关系，并生成中文离线 HTML 报告。

不要预先接受下面任何历史技术判断。背景信息用于限定调查范围；候选函数、文件和调用链只是检索线索，必须以你实际读取的源码为准。

# 一、研究目标与边界

我想弄清楚：

1. “底层已经支持设备内存 IPC”“TorchNPU 已适配 Tensor IPC”“LMCache-driven 已适配”分别是什么意思。
2. 从 LMCache / vLLM 到 TorchNPU、CANN、HDK Driver/Firmware，各层具体在哪里完成适配。
3. LMCache-Ascend 为什么会与 TorchNPU 的共享接口产生依赖，能否从实际调用代码证明。
4. 默认分配路径、expandable segments / VMM 路径和设备 Event IPC 是不是不同的实现分支。
5. 之前提到的“24.x 已经支持 IPC”究竟对应什么功能；它与“某份 TorchNPU IPC 指南要求 HDK 25.3.RC1”是否属于不同层级或不同使用条件。
6. 已有证据能否限定当前服务的阻断范围，哪些事情仍不能由源码或版本号单独证明。

核心要求是“解释＋证据”：每个关键结论都要能点击跳转到具体源码片段或官方文档位置。

本次只做静态源码与文档调查，不运行 NPU 单元实验，不部署服务，不修改服务器。

# 二、已知环境：这是用户报告的信息，不是你本机的检测结果

目标硬件：
- Ascend 910B3。
- 此前曾误写成 910B4，现已明确更正为 910B3。
- 目标场景：vLLM 四卡 TP=4，结合 LMCache 的 MP 部署。

用户已经确认：
- engine_driven 模式可以启动对应的 vLLM 服务。
- 这不代表已验证 lmcache_driven。
- 不需要再次调查“auto 到底选择了哪个模式”。
- vLLM Worker 与 LMCache Server 完全共用同一套软件环境。

版本信息：
- Driver：25.2.0，注意不是 25.0.2。
- Firmware：7.7.0.6.236。
- Driver / Firmware Compatibility：OK。
- CANN：9.1.0。
- Python：3.12.13。
- torch：2.10.0+CPU。
- torch-npu：2.10.0.post4。
- vllm：0.23.0+empty。
- vllm-ascend：0.23.0。
- lmcache：0.1.dev2316。
- lmcache-ascend：0.1.dev167。
- LMCACHE_MP_TRANSFER_MODE：查询 Shell 中为 UNSET。
- PYTORCH_NPU_ALLOC_CONF：查询 Shell 中为 UNSET。
- PYTORCH_ALLOC_CONF：尚未取得。
- LMCache 与 LMCache-Ascend 的实际 commit、补丁内容和加载过程：尚未取得。

这些开发版版本字符串不能直接映射成某个公开 commit，也不能仅凭“0.1”判断新旧。

历史调查记录中曾出现：
- TorchNPU 跨进程 Tensor 测试失败。
- 一次 expandable segments 配置测试记录：
  AclrtMemImportFromShareableHandle → 507899
  halMemImportFromShareableHandle → drvRetCode=17

这些只是用户提供的历史现象，不是你本机复现结果。
不要把它们直接解释为“910B3 硬件没有任何 IPC 能力”。

# 三、工作方式与工程约束

在当前工作目录下新建独立子目录，例如：

ascend-ipc-source-review/

只在这个目录里创建源码副本、索引和报告。

请遵守：
- 先复用已有的合适源码副本；不要覆盖或清理用户已有修改。
- 默认浅克隆或部分克隆，只获取本任务需要的分支、标签和文件。
- 不默认递归下载全部子模块。
- 只有确实需要确认接口引入或修复历史时，才针对相关路径补充历史。
- 不安装 torch、torch-npu、vLLM、CANN；不构建大型工程。
- 不执行被调查仓库中的安装脚本、自动升级脚本或全量测试。
- 不导入这些包来触发运行时初始化、插件注入或 NPU 初始化；静态阅读优先。
- 不改系统 PATH，不使用管理员权限，不修改全局 Agent 规则。
- 不连接生产服务器，不读取凭据，不请求完整环境变量、启动参数或业务日志。
- 不做全仓库文件哈希、无关代码扫描、重复验证和无关重构。
- Git commit 用于源码定版和行号定位，这是必要信息；不要扩展成大规模指纹工程。
- 按当前 PC 实际系统使用 PowerShell、Bash 或 Python 标准库，不假定必须有 WSL。
- 主要证据覆盖后完成交付，不把本任务扩展成部署平台、课程系统或复杂前端工程。

某个仓库、文档或版本获取失败时，记录失败和证据缺口，继续完成其他可完成部分；不要伪造读取结果。

# 四、需要取得的仓库及版本策略

## 4.1 核心仓库

TorchNPU：
https://github.com/Ascend/pytorch.git

同时核对官方主仓库／镜像关系：
https://gitcode.com/Ascend/pytorch

LMCache：
https://github.com/LMCache/LMCache.git

LMCache-Ascend：
https://github.com/LMCache/LMCache-Ascend.git

## 4.2 集成入口仓库

vLLM：
https://github.com/vllm-project/vllm.git

vLLM-Ascend：
https://github.com/vllm-project/vllm-ascend.git

这两个仓库只需围绕插件加载、KV Connector、缓存注册和 LMCache 集成读取，不研究完整模型执行系统。

## 4.3 底层仓库

CANN Runtime 的候选入口：
https://gitcode.com/cann/runtime

请确认当前官方仓库和有效 clone 地址，记录实际来源及重定向后的地址。不要假定它必然包含用户所安装的 HDK Driver 或 Firmware 源码。

不默认克隆完整 upstream PyTorch。只有 TorchNPU 到 PyTorch 的关键连接无法从已有材料确认时，才按需补取相应版本的具体文件。

## 4.4 定版规则

每个实际读取的仓库必须记录：
- 仓库地址及本地相对目录。
- branch/tag。
- 完整 commit SHA。
- 获取日期。
- 是否存在本地修改。
- 与用户实际安装版本的关系。

TorchNPU：
优先查找能明确对应 2.10.0.post4 的官方标签、发布记录或提交信息。
不要凭空构造标签名。
如果只能找到 v2.10.0 或其他参考版本，明确标注“参考实现，尚未证明与 post4 安装包完全一致”。

vLLM / vLLM-Ascend：
优先核对 0.23.0 对应发布标签。

LMCache / LMCache-Ascend：
由于用户安装 commit 未知，先读取插件中的上游版本约束、子模块、构建元数据和补丁说明。
如果找到 LMCACHE_UPSTREAM_TAG 等约束，应优先调查与该插件配套的 LMCache 源码，不能随意拿 LMCache 最新 dev 拼接插件 main。

可以追加当前开发分支作为对照，但必须分开展示：
A. 有依据的配套参考组合。
B. 当前开发分支的接口形态。
C. 用户安装版本仍待确认的部分。

绝不把 A、B 的代码拼成用户服务器上已经确定存在的一条链。

# 五、需要核查的源码链路

以下路径和符号是候选检索线索。不存在、移动或改名时，搜索真实实现并说明变化，不要照抄。

## C01：服务集成和缓存注册入口

优先查找：
- vLLM 中的 LMCache KV Connector。
- vLLM-Ascend 如何加载相关插件或适配。
- lmcache/integration/vllm/vllm_multi_process_adapter.py。
- register_kv_caches。
- _send_register_kv_caches_request。
- create_transfer_context。
- engine_driven / lmcache_driven 的具体分支。

回答：
- 哪个对象持有 KV Cache？
- 哪个函数触发缓存注册？
- 谁选择传输模式？
- 共享句柄在哪个进程生成、发送、接收和恢复？
- 哪些步骤发生在启动、注册、首次 STORE 或 RETRIEVE 时？
- engine_driven 与 lmcache_driven 在所查版本里实际分叉在哪里？

不能只展示函数定义，需要指出调用方和可达条件。

## C02：LMCache-Ascend 的适配入口

候选文件：
- lmcache_ascend/__init__.py
- lmcache_ascend/v1/multiprocess/custom_types.py

候选符号：
- _patch_multi_process
- AscendIPCWrapper
- CudaIPCWrapper
- _share_npu_
- _new_shared_npu
- to_tensor
- set_

重点核查：
- 是否真的替换了某个 CudaIPCWrapper 引用。
- 补丁函数在哪里被调用，是否有环境或运行时条件。
- 是否可能受到 import 顺序和已缓存引用影响。
- wrapper 如何保存 dtype、shape、stride、storage offset、设备身份。
- 发送侧是否实际调用 TorchNPU Storage 共享。
- 接收侧如何恢复 Storage 并重建 Tensor。
- 单独存在一个类，与该类实际被服务使用，有什么区别。

不要将“看见一条赋值语句”直接写成“用户服务的补丁已经生效”。

## C03：DeviceSpec 与补丁式适配的区别

候选位置：
- lmcache/v1/platform/
- lmcache/v1/platform/base/device_spec.py
- lmcache/v1/platform/base/ipc_wrapper.py
- lmcache/v1/platform/base/event_ipc.py
- lmcache/v1/platform/base/cache_context.py

候选接口：
- ipc_wrapper_cls
- event_ipc_backend
- create_cache_context
- current_device_spec
- resolve_kv_wrapper_factory
- is_handle_transfer_available

回答：
- 这些接口在哪个被核查版本中存在？
- 默认行为是什么？
- NPU 实现在哪里注册？
- 与 C02 的补丁式架构是什么关系？
- 是否属于不同年代、分支或配套组合？

不能因为旧插件没有新版 DeviceSpec 属性，就直接断言它完全没有适配。

## C04：TorchNPU 的 Python 适配

候选文件：
- torch_npu/__init__.py
- torch_npu/utils/storage.py
- torch_npu/multiprocessing/reductions.py

候选符号：
- _add_storage_methods
- _share_npu_
- _new_shared_npu
- _npu_reduce_tensor
- rebuild_npu_tensor
- _add_reductions_methods

回答：
- NPU Storage 方法在哪里添加到 PyTorch 对象上？
- Python 方法怎样转入 torch_npu._C？
- Queue / Pipe 的 Tensor 序列化怎样进入 NPU reducer？
- LMCache wrapper 和 Queue 实验是否会在 Storage 共享入口汇合？
- 两者哪些步骤相同，哪些消息传输或元数据步骤不同？
- reducer 的注册与初始化在哪里发生？

不要把 Queue、Pickle 本身描述成设备内存共享实现。

## C05：TorchNPU 的 C++ Storage 与分配器

候选文件：
- torch_npu/csrc/ipc/StorageSharing.cpp
- torch_npu/csrc/core/npu/NPUCachingAllocator.cpp

候选符号：
- THNPStorage_shareNpu
- THNPStorage_newSharedNpu
- shareIpcHandle
- getIpcDevPtr
- fromShared
- SHAREABLE_NPU_MALLOC
- SHAREABLE_NPU_EXPANDABLE_SEGMENT

分别核查两条候选路径：

A. 非 expandable segment 路径
- AclrtIpcMemGetExportKey
- AclrtIpcMemImportByKey

B. expandable segments / VMM 路径
- AclrtMemExportToShareableHandle
- AclrtMemImportFromShareableHandle
- 地址预留、映射和解除映射等相关调用

必须找到真实分支条件，不要只根据环境变量名称断言一定进入某条分支。

同时解释：
- Tensor、Storage、allocator block 的区别。
- allocation base、storage offset、tensor offset 分别起什么作用。
- 引用计数与生产者生命周期由谁管理。
- 是否可能发生分配器回退。
- 多次导入、导入缓存和多个消费者怎样影响实际调用次数。

不要把 data_ptr() 一律叫作“物理地址”，也不要要求两个进程中的虚拟地址数值相等。

## C06：TorchNPU → CANN → Driver 的边界

候选文件：
- torch_npu/csrc/core/npu/interface/AclInterface.cpp
- 对应头文件及动态符号加载实现
- CANN Runtime 中实际的 ACL / Runtime / Driver 调用文件

候选符号：
- TORCH_NPU_GET_FUNC
- AclrtMemImportFromShareableHandle
- aclrtMemImportFromShareableHandle
- rtMemImportFromShareableHandle
- halMemImportFromShareableHandle

回答：
- 哪一层只是封装或动态符号查找？
- 哪一层真正发起接口调用？
- 函数不存在与函数返回错误分别在哪段代码处理？
- 导出 flags、PID 校验、白名单、设备号、进程和内存类型有哪些条件？
- 历史错误码 507899、drvRetCode=17 能在相应版本的哪里核实？
- 错误码定义的来源是否与目标软件版本对应？

源码能追到哪一层就写到哪一层。
找不到对应 Driver/Firmware 实现时，明确画出证据边界，不要补写“硬编码拒绝”“硬件没有 MMU 映射能力”等未经证实的解释。

不要为了绕过限制而建议关闭安全校验。

## C07：设备 Event IPC 的并行链

候选文件：
- torch_npu/npu/streams.py
- torch_npu/csrc/core/npu/NPUEvent.cpp
- torch_npu/csrc/ipc/NPUIPCTypes.cpp
- torch_npu/csrc/core/npu/interface/AclInterface.cpp
- LMCache 实际 Event 后端

候选符号：
- Event(interprocess=True)
- ipc_handle
- from_ipc_handle
- record / wait / query / synchronize
- AclIpcGetEventHandle
- AclIpcOpenEventHandle
- IsSupportIpcEvent

必须分别回答：
- Python API 存在与运行时支持检查有什么区别？
- Event 导出和恢复的 C++ / CANN 调用在哪里？
- Tensor IPC 内部是否存在 Event 不可用时改用 stream synchronization 的分支？
- 该回退是否适用于 LMCache 使用的独立 Event 接口？
- 为什么 Tensor 共享通过不必然证明 Event IPC 通过？
- 哪个所查 LMCache 版本要求独立的 Event 后端？

Python multiprocessing.Event、设备 Event IPC、流同步必须分开解释。

## C08：Host 内存、IPC_LOCK 与设备 IPC 的概念隔离

只围绕本题所需范围，解释：
- Host pinned memory / HostRegister。
- CPU shared memory。
- Docker / Linux 的 IPC_LOCK 权限。
- Device memory IPC。
- Device Event IPC。

核查 LMCache-Ascend README 中“Driver >=24.1.0”及 HostRegisterError 的实际上下文。

不能因为名称中出现 IPC，就认定这些功能等价。
不能把 CPU 缓存后端直接理解为完全不需要设备内存 IPC。
也不能把“共享访问设备内存”写成“整个缓存保存和回填过程完全没有复制”。

# 六、版本说法的专项核查

下面是线索，不是预设结论：

官方 TorchNPU 内存 IPC 指南：
https://www.hiascend.com/document/detail/zh/Pytorch/2600/ptmoddevg/Frameworkfeatures/docs/zh/framework_feature_guide_pytorch/memory_sharing_ipc.md

官方版本配套表候选：
https://www.hiascend.com/doc_center/source/zh/ModelZoo/pytorchframework/pies/pies_00003.html

CANN 9.1.0 共享句柄导入接口候选：
https://www.hiascend.com/doc_center/source/zh/CANNCommunityEdition/910/API/runtimeapi/aclcppdevg_03_0121.html

LMCache 设备适配文档：
https://docs.lmcache.ai/developer_guide/extending_lmcache/adding_a_new_device_backend.html

vLLM-Ascend 0.23.0 安装说明：
https://docs.vllm.ai/projects/ascend/en/v0.23.0/installation.html

补充技术文章线索：
https://cann.csdn.net/69afcc5454b52172bc60362f.html

优先读取官方文档、项目仓库、维护者发布说明。
补充文章的作者身份和措辞也要核实，不能把技术文章自动等同于正式兼容性承诺。

制作“版本—能力—证据范围”表，分开核查：
- HDK 24.1.RC1 与 CANN 8.0.RC1 的配套关系。
- LMCache-Ascend 的 Driver 24.1.0 通用要求。
- 某组底层 IPC API 在哪个已核版本中存在。
- HDK 25.3.RC1 的 TorchNPU 内存 IPC 使用条件。
- 所谓“多次导入修复”具体修复什么，能否找到发布说明或代码依据。
- 用户当前 CANN 9.1.0 / TorchNPU 2.10.0.post4 对整套 HDK 的配套要求。
- Device Event IPC 有没有独立要求。

表中要明确区分：
1. 接口在某版本存在。
2. 特定调用方式得到支持或修复。
3. 框架文档规定的使用基线。
4. 某个完整组合的测试记录。
5. 最早支持版本是否真正查明。

只能证明“某版本已存在”，就这样写，不要写成“首次从该版本支持”。
不能从“25.3.RC1 修复某问题”反推出此前所有 IPC 都不存在。
不能从 Driver/Firmware Compatibility=OK 推出所有特性版本条件都满足。
不能从一条新版本文档倒推出旧版二进制的全部行为。

如果官网页面只返回导航，或只找到搜索摘要，注明未读取到正文，不能当作已经核验完整条款。

不需要遍历所有历史版本。围绕上述争议和实际调用链做必要追溯即可。

# 七、源码证据与行号的硬要求

为关键证据分配稳定编号 E001、E002……

每项源码证据至少记录：
- 证据编号及一句话结论。
- 所属软件层、调用链和参考版本组合。
- 仓库地址。
- 完整 commit SHA。
- 相对文件路径。
- 函数或类名。
- 准确起止行号。
- 原始源码片段。
- 这段代码能证明什么、不能证明什么。
- 固定 commit 的在线永久链接。
- 报告内对应的离线锚点。

GitHub 链接采用：
https://github.com/OWNER/REPO/blob/FULL_COMMIT/path/to/file#L开始-L结束

GitCode 等平台使用它实际支持的格式，验证后再写，不要猜测。
外站不支持准确行号链接时，保留固定提交文件链接，并以报告内编号片段作为精确定位。

行号必须来自对应 commit 的真实文件：
- 优先用 git show COMMIT:path 读取内容并从第 1 行编号。
- 不用搜索摘要的行号。
- 不用网页渲染行号代替源码行号。
- 不用修改后的工作树行号冒充 upstream commit 行号。
- 不把不同文件、不同 commit 的连续片段伪装成同一段源码。

展示时将“原始源码”和“中文注释／伪代码”分开。
不要往原始片段插入注释后继续声称它保留原始行号。
长函数只截关键窗口，必要时提供多个带独立行号的片段。

调用链每条关键箭头应有调用点证据。
动态注册、猴子补丁、函数指针或条件分派，用不同样式标明。
只发现函数定义但未确认调用关系时，标“定义已定位，调用关系待确认”。

状态建议分成两轴，避免混淆：
- 证据来源：源码 / 官方文档 / 维护者记录 / 用户报告 / 推断。
- 适用范围：已核参考版本 / 用户安装版待对应 / 运行结果未知。

静态源码核查完成，不得记作硬件测试 PASS。

# 八、HTML 报告的具体要求

主交付物为一个中文、可直接双击离线打开的 HTML 文件。
使用原生 HTML/CSS/少量 JavaScript，优先内联 SVG 绘图。
不要用 React、Next.js、服务器、CDN、在线字体或运行时联网接口。

报告需要包含：

## 8.1 首页摘要
先用容易理解的语言回答：
- 层级依赖关系是否成立。
- “有底层接口”和“有完整上层适配”的区别。
- 已经查明什么，尚未查明什么。
- 用户实际安装源码尚未对齐的地方。

不要把不确定性藏在最后。

## 8.2 软件层级图
展示：
vLLM / LMCache 集成
→ LMCache-Ascend / 设备后端
→ TorchNPU Python
→ TorchNPU C++
→ CANN
→ Driver/Firmware
→ 910B3

这是软件层，不要叫 CPU 缓存 L1/L2。
每层说明对象、职责、适配位置，点击可跳到证据。

## 8.3 双进程内存共享图
至少画出：
- Worker 进程。
- Server 进程。
- 共享的设备内存。
- 导出与导入句柄。
- Tensor 元数据。
- 进程各自的地址映射。
- 内存和对象生命周期。
- 控制消息与实际数据搬运的区别。

画图必须与所选代码版本一致。
不要默认两个进程的虚拟地址相同，也不要默认 TP=4 等于四张卡共享同一块 HBM。

## 8.4 两条分配器路径对照
把 IPC key 路径和 VMM/shareable-handle 路径分开。
显示实际分支条件、导出接口、导入接口、映射和释放动作。

## 8.5 Event IPC 并行链
与内存 IPC 并列展示：
- 内存 IPC 回答“能否访问”。
- Event IPC 回答“何时可以安全访问”。

标明 TorchNPU Tensor 共享内部的同步处理与 LMCache 独立 Event 后端是否是不同路径。

## 8.6 适配点与源码卡片
每个关键适配点都能展开：
原始带行号源码＋中文逐段解释＋上下游调用＋证据范围＋永久链接。

默认折叠次要实现，避免页面成为几千行代码墙。
关键行可以高亮，但不能只贴截图，代码应能选择复制。

## 8.7 版本与能力矩阵
将“历史版本说法”“官方支持基线”“已见代码实现”“用户安装版”“运行待验证”分开呈现。

## 8.8 历史判断复核
逐条判断以下说法的证据是否足够：
- “24.x 支持 IPC，所以 TorchNPU 和 LMCache-driven 必然支持。”
- “HDK 25.2.0 根本没有 IPC。”
- “调用在驱动层失败，所以已经证明硬件不支持。”
- “Tensor IPC 通过，所以 Event IPC 一定通过。”
- “engine_driven 启动成功，所以 lmcache_driven 依赖也满足。”
- “找不到新版 DeviceSpec 属性，所以没有 Ascend 适配。”

允许保留未知，不要求强行给出全局支持／不支持结论。

## 8.9 未决项和后续验证边界
只列最有判别力的缺口。
本次不生成新的大套测试工具，不执行实验。
可以说明后续需要内存共享、Event、真实 wrapper 或服务集成中的哪一项验证。

页面提供固定目录、页内跳转和源码折叠即可。
正文适合桌面阅读，图中文字足够大，代码横向滚动不撑坏页面。
外部源码链接联网时可用，但正文、图示和代码证据在断网时仍然可读。
不要通过 fetch 本地 JSON 才能显示正文，以免 file:// 打开失败。

# 九、交付目录与最小验证

建议交付结构：

ascend-ipc-source-review/
├── repos/                  实际取得的源码
├── report.html             主报告，离线可读
├── evidence_index.json     仓库定版信息、证据、行号及链接索引
└── README.md               打开方式、范围、版本差异和未决项

JSON 索引保持简单，供以后补充用户真实 commit 时使用，不要设计数据库。
必要时保留一个小型生成脚本，但不要增加与交付无关的文件。

完成前只做必要检查：
- 关键引用文件和行号范围确实存在。
- 展示片段与记录的 commit 内容一致。
- 报告内关键锚点能跳转。
- HTML 能离线显示，不依赖外部脚本或字体。
- 不同参考版本没有被误拼成一条真实调用链。
- 用户报告、静态证据和运行结论清楚区分。

有浏览器工具时实际打开检查一次，修复明显溢出、遮挡或无法展开。
没有浏览器工具就做静态检查并说明限制，不要宣称已经浏览器验证。

不编译 CANN/TorchNPU，不拉取模型，不跑 GPU/NPU 实验，不启动 vLLM，不做全量测试。

# 十、最终回复给我的内容

完成后简短说明：
1. report.html 的实际路径和打开方式。
2. 已克隆的仓库及固定提交。
3. 最重要的几项源码核查结论。
4. 哪些结论仅适用于参考版本，不能直接对应我的安装包。
5. 还缺哪些最少的信息才能进一步对齐。

受限服务器上的信息只能手工回报。
如果必须补充信息，只提出短字段，例如：
- M01：LMCache commit 前 12 位。
- M02：LMCache-Ascend commit 前 12 位。
- M03：某个补丁入口是否存在，YES/NO。
不要索要完整日志、全量源码、完整环境变量或业务文件。

不要只回复“建议怎么做”。
请在当前 PC 上实际完成可完成的源码取得、核查、索引和 HTML 生成；无法核实的地方如实标记，不要为了报告完整而补造答案。
````
可以。**后面不需要向容器传检查包，直接在 SSH 的 Bash 里粘贴命令执行。** 当前基线也明确为：**910B3 上 `engine_driven` 能启动；我们要单独验证 `lmcache_driven` 增加的依赖。**

## 1. 官方《内存共享（IPC）》链接

前面引用的是这一页：

**[昇腾官方：《内存共享（IPC）》— PyTorch 框架特性指南](https://www.hiascend.com/document/detail/zh/Pytorch/2600/ptmoddevg/Frameworkfeatures/docs/zh/framework_feature_guide_pytorch/memory_sharing_ipc.md)**

在页面中查找 **`25.3.RC1`**。该官方页面的检索内容明确包含：

> 该特性仅支持在Ascend HDK 25.3.RC1及以上且CANN 8.3.RC1及以上版本使用。:chatgpt-content-reference{index="0"}

官网采用动态加载；我这次直接抓取页面只获得了网站导航，但官方检索结果中可以核对到上述句子。

另外，这篇由 **“昇腾CANN”** 发布的技术说明能够直接读到更详细的原因：

**[《基于 torch_npu 的 IPC 特性介绍》](https://cann.csdn.net/69afcc5454b52172bc60362f.html)**

定位到 **“torch_npu IPC版本约束” → “HDK多次Import支持限制”**。它说明 `25.3.RC1` 修复了多次调用 `aclrtMemImportFromShareableHandle` 时无法成功导入的问题。**所以，这个版本要求不能被解释成“此前完全不存在底层 IPC”。**:chatgpt-content-reference{index="1"}

## 2. `engine_driven` 能启动，作为已知基线保留

现在不再排查“auto 到底选了什么”，直接按你确认的结果记录：

```text
硬件：910B3
engine_driven：服务可以启动
lmcache_driven：相关能力待验证
```

两条路径的要求不同。例如，当前 LMCache 文档明确指出：**设备 Event IPC 后端是 `lmcache_driven` 的要求，而 `engine_driven` 不要求这项能力。** 因此，前者需要单独测试，但无需为了测试而改动已经工作的后者。:chatgpt-content-reference{index="2"}

---

## 3. 直接通过 Bash 执行第一轮测试

**这轮只测下面四项，S01 和补丁绑定检查接在后面：**

| 编号 | 测试内容 |
|---|---|
| **T00** | 当前 Python 环境能否完成基础 NPU 运算 |
| **T01** | 当前分配器配置下，能否跨进程共享 NPU Tensor，并让原进程看到另一进程的修改 |
| **T02** | 显式请求 `expandable_segments:True` 后，重复同一内存共享测试 |
| **T05** | NPU Event 能否跨进程导出、导入、等待、查询和同步 |

下面的 Bash **只在容器本地 `/tmp` 生成一个临时 Python 脚本和日志，不下载东西，不安装软件，不停止现有服务**。使用临时脚本，是因为 `spawn` 子进程需要重新导入主模块，不适合把整个多进程测试直接写成 `python3 - <<'PY'`。:chatgpt-content-reference{index="3"}

请在**服务实际使用的容器、用户和 Python 环境**中执行，并选择空闲卡或维护窗口。测试数据很小，但初始化 NPU 运行时仍然会占用资源。

### A. 先粘贴这一整段：准备测试代码

前两行可以调整：

```bash
IPC_PY="$(command -v python3)"
IPC_DEV=0
```

`IPC_DEV=0` 指的是**容器内逻辑卡号**。如果服务使用另一个 Python，把 `IPC_PY` 改为那个解释器的完整路径。

以下代码从头到最后的 `PY` 一起复制：

```bash
IPC_PY="$(command -v python3)"
IPC_DEV=0
IPC_DIR="$(mktemp -d /tmp/910b3-ipc.XXXXXX)"

printf 'V16B=%s\n' "${PYTORCH_ALLOC_CONF:-UNSET}"
printf 'LOCAL_LOG_DIR=%s\n' "$IPC_DIR"

[ -n "$IPC_DIR" ] && [ -d "$IPC_DIR" ] &&
cat > "$IPC_DIR/probe.py" <<'PY'
import gc
import multiprocessing as mp
import os
import sys
import traceback

STAGE = "IMPORT"

def mark(s):
    global STAGE
    STAGE = s
    print("STAGE=" + s, flush=True)

def require(ok, message):
    if not ok:
        raise RuntimeError(message)

def setup(d, side):
    global torch, npu
    mark(side + "_IMPORT")
    import torch
    import torch_npu
    npu = torch_npu.npu
    mark(side + "_DEVICE")
    npu.set_device(d)

def guarded(fn, *args):
    try:
        fn(*args)
    except Exception as e:
        print("FAIL_AT=" + STAGE + ":" + type(e).__name__, flush=True)
        traceback.print_exc()
        raise SystemExit(1)

def receive(kind, d, conn):
    setup(d, "C")
    mark("RECV_IMPORT")
    obj = conn.recv()

    if kind == "T05":
        mark("EVENT_IMPORT")
        event = npu.Event.from_ipc_handle(torch.device("npu", d), obj)
        stream = npu.Stream()

        mark("EVENT_WAIT")
        event.wait(stream)
        with npu.stream(stream):
            result = torch.ones(128, device=f"npu:{d}") + 1
        stream.synchronize()

        mark("EVENT_QUERY_SYNC")
        require(event.query(), "EVENT_NOT_COMPLETE")
        event.synchronize()
        require(result.sum().item() == 256, "EVENT_VALUE")
        del event, result
    else:
        mark("READ")
        require(obj.device.type == "npu" and obj.data_ptr() != 0, "BAD_TENSOR")
        require(tuple(obj.shape) == (1024,), "BAD_SHAPE")
        require(torch.equal(obj.cpu(), torch.ones(1024)), "READ_VALUE")

        mark("WRITE")
        obj.add_(1)
        npu.synchronize()

    del obj
    gc.collect()
    conn.close()

def child(kind, d, conn):
    guarded(receive, kind, d, conn)

def main(kind, d):
    setup(d, "P")
    print("ALLOC_NPU=" + os.getenv("PYTORCH_NPU_ALLOC_CONF", "UNSET"))
    print("ALLOC_COMMON=" + os.getenv("PYTORCH_ALLOC_CONF", "UNSET"))

    if kind == "T00":
        mark("COMPUTE")
        x = torch.ones(1024, device=f"npu:{d}")
        require((x + 1).sum().item() == 2048, "COMPUTE_VALUE")
        return

    require(kind in ("T01", "T02", "T05"), "BAD_CASE")

    if kind == "T05":
        mark("EVENT_CREATE")
        event = npu.Event(interprocess=True)
        stream = npu.Stream()
        with npu.stream(stream):
            work = torch.ones(128, device=f"npu:{d}") + 1
            event.record(stream)
        mark("EVENT_EXPORT")
        payload = event.ipc_handle()
    else:
        mark("ALLOC")
        x = torch.ones(1024, device=f"npu:{d}")
        npu.synchronize()
        payload = x

    ctx = mp.get_context("spawn")
    parent, peer = ctx.Pipe()
    p = ctx.Process(target=child, args=(kind, d, peer))

    mark("SPAWN")
    p.start()
    peer.close()
    try:
        mark("SEND_EXPORT")
        parent.send(payload)

        mark("WAIT_CHILD")
        p.join(120)
        require(not p.is_alive(), "CHILD_TIMEOUT")
        require(p.exitcode == 0, "CHILD_FAILED")

        if kind != "T05":
            mark("VERIFY_SHARED_WRITE")
            require(torch.equal(x.cpu(), torch.full((1024,), 2.0)), "NOT_SHARED")
    finally:
        parent.close()
        if p.is_alive():
            p.terminate()
            p.join(5)
            if p.is_alive():
                p.kill()
                p.join(5)

if __name__ == "__main__":
    guarded(main, sys.argv[1], int(sys.argv[2]))
PY
```

这里的内存测试保留生产者及原始 Tensor，直到消费者退出；消费者不仅读取，还会修改数据，由生产者检查修改是否可见。Event 测试调用的是 TorchNPU 提供的 `interprocess=True`、`ipc_handle()` 和 `from_ipc_handle()` 等接口。:chatgpt-content-reference{index="4"}

### B. 再粘贴这一段：定义执行命令

它会将完整日志留在刚才的临时目录，终端只输出短结果。每项最多运行约三分钟，超时后终止的是**本次测试的进程**。

```bash
ipc_run() (
    set +e

    case "${1:-}" in
        T00|T01|T02|T05) ;;
        *) echo 'USE: ipc_run T00|T01|T02|T05'; exit 2 ;;
    esac

    k="$1"
    command -v timeout >/dev/null || {
        echo "$k SKIP NO_TIMEOUT"
        exit 77
    }
    [ -f "$IPC_DIR/probe.py" ] || {
        echo "$k SKIP NO_SCRIPT"
        exit 77
    }

    cmd=(env)
    if [ "$k" = T02 ]; then
        cmd+=(-u PYTORCH_ALLOC_CONF
              PYTORCH_NPU_ALLOC_CONF=expandable_segments:True)
    fi

    log="$IPC_DIR/$k.log"
    timeout -k 5s 180s "${cmd[@]}" "$IPC_PY" -u \
        "$IPC_DIR/probe.py" "$k" "$IPC_DEV" >"$log" 2>&1
    rc=$?

    note=''
    if [ "$k" = T02 ] &&
       grep -q 'expandable_segments setting failure' "$log"; then
        note=' VMM_FALLBACK'
    fi

    if [ "$rc" -eq 0 ]; then
        echo "$k PASS$note"
    else
        hint=$(grep -m1 '^FAIL_AT=' "$log")
        [ -n "$hint" ] ||
            hint=$(grep '^STAGE=' "$log" | tail -n1)
        codes=$(grep -Eo \
            'drvRetCode=[0-9]+|error code is [0-9]+' "$log" |
            sort -u | head -n2 | tr '\n' ' ')
        echo "$k FAIL rc=$rc $hint $codes$note"
    fi

    exit "$rc"
)
```

T02 会在**自己的测试进程中**移除 `PYTORCH_ALLOC_CONF`，再设置 NPU 专用分配器变量，避免二者同时存在产生配置冲突；不会修改当前服务的环境。TorchNPU 源码确实对这两个变量同时设置进行了检查。:chatgpt-content-reference{index="5"}

**A、B 两段在同一个 SSH 会话里执行一次即可。** 重新连接后，Shell 中的这些变量和函数不会自动保留，需要重新准备。

### C. 现在一项一项运行

先运行基础运算：

```bash
ipc_run T00
```

**T00 失败就先停在这里。**

T00 成功后，运行当前配置下的内存共享：

```bash
ipc_run T01
```

再独立运行设备事件测试。即使 T01 失败，也可以继续这一项：

```bash
ipc_run T05
```

**T01 失败时，再运行分配器对照：**

```bash
ipc_run T02
```

---

## 4. 你只需要回报这几行

```text
V16B=UNSET / 实际值

T00=PASS / FAIL
T01=PASS / FAIL
T05=PASS / FAIL
T02=未运行 / PASS / FAIL
```

如果失败行很短，可以照抄，例如：

```text
T02 FAIL RECV_IMPORT 17/507899
```

不用抄完整堆栈，也不用发送日志文件。

结果解释上保留三个边界：

| 输出 | 含义 |
|---|---|
| **`FAIL rc=124`** | 超时，不直接等于接口或硬件不支持 |
| **`T02 PASS VMM_FALLBACK`** | 测试通过，但日志出现分配器回退提示，**不能计作 VMM 路径通过** |
| **`T05 PASS`** | Event IPC 基础接口闭环通过，**不等于实际 KV Cache 的全部异步执行顺序已经验证** |

T02 本身仍是 **TorchNPU 测试，不是绕过框架的原生 CANN 测试**；而且仅请求环境变量，也不能单独证明内部绝无回退。

**以上命令已做 Bash/Python 语法和失败输出检查，没有在真实 NPU 上实测。第一轮先取得这四项结果，再决定下一步查内存导入、Event IPC，还是 LMCache 的 Ascend 补丁绑定。**
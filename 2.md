可以。我们现在按 **910B3、现有 `auto` 模式已经能启动、先不升级或改动现有环境** 这三个前提推进。

这次把工作拆成两类编号：

- **`Vxx`：版本与运行环境信息**，你只需要回报版本号和少量配置。
- **`Sxx / Txx`：检查和测试**，你只需要回报 `PASS / FAIL / SKIP`；失败时能再附一个短阶段或数字错误码就足够。

我已经准备好明文脚本，包含版本采集、单项执行、超时控制和本地日志保存：

**:chatgpt-content-reference{index="15"}[检查包：910b3_ipc_checks_v1.zip](sandbox:/mnt/data/910b3_ipc_checks_v1.zip)**  
:chatgpt-content-reference{index="16"}[使用说明](sandbox:/mnt/data/910b3_ipc_checks/README.md) · :chatgpt-content-reference{index="17"}[Python 用例源码](sandbox:/mnt/data/910b3_ipc_checks/ipc_probe.py) · :chatgpt-content-reference{index="18"}[执行脚本](sandbox:/mnt/data/910b3_ipc_checks/run_probe.sh) · :chatgpt-content-reference{index="19"}[版本采集脚本](sandbox:/mnt/data/910b3_ipc_checks/collect_versions.py)

**这些脚本经过了语法和异常输出检查，但没有在真实 NPU 上运行验证。** 它们是接下来收集证据的工具，不是已经通过的实验结果。

# 第一部分：通过 SSH 查询版本

## 1. 先在宿主机查 Driver、Firmware

从你的电脑连接服务器：

```bash
ssh 用户名@服务器IP
```

进入后执行：

```bash
npu-smi info -l
```

查看设备编号，再对其中一张卡执行：

```bash
npu-smi info -t board -i 0 | grep -E 'Software Version|Firmware Version|Compatibility'
```

这里的 `0` 换成实际设备编号。官方说明中，`Software Version` 是驱动版本，`Firmware Version` 是固件版本，`Compatibility` 表示二者的兼容性。:chatgpt-content-reference{index="0"}

你只需要记下：

```text
V01 Driver=……
V02 Firmware=……
V03 Compatibility=……
```

**不用发完整 `npu-smi` 输出，不用发序列号、设备地址或进程列表。**

## 2. 进入真正运行服务的容器

如果服务运行在 Docker 中，先查看：

```bash
docker ps --format 'table {{.Names}}\t{{.Image}}'
```

查看目标容器的相关配置：

```bash
docker inspect --format 'ipc={{.HostConfig.IpcMode}} pid={{.HostConfig.PidMode}} shm={{.HostConfig.ShmSize}}' 容器名
```

记为：

```text
V14 ipc=…… pid=…… shm=……
```

然后进入容器：

```bash
docker exec -it 容器名 bash
```

**接下来必须使用服务实际使用的用户、Python 环境和相关环境变量。** `docker exec` 启动的新 Shell，不一定具有启动脚本后来临时设置的环境变量；仅仅“进入同一个容器”还不够。:chatgpt-content-reference{index="1"}

先在本地确认解释器：

```bash
command -v python3
python3 -c 'import sys; print(sys.executable)'
```

如果你的服务使用虚拟环境里的另一个 Python，下面的 `python3` 都换成那个解释器。

**还有一个需要区分的情况：** 如果 vLLM Worker 和 LMCache Server 分别运行在两个容器或两个 Python 环境里，版本需要各查一份，分别标成 `W-V07`、`S-V07`。如果它们完全共用同一套环境，告诉我“同环境”即可。

## 3. 查 Python、TorchNPU、vLLM、LMCache 版本

下面这段可以直接粘到 SSH，**不依赖附件**：

```bash
python3 - <<'PY'
import sys
from importlib.metadata import version, PackageNotFoundError

print("V05 Python=" + sys.version.split()[0])
names = [
    "torch",
    "torch-npu",
    "vllm",
    "vllm-ascend",
    "lmcache",
    "lmcache-ascend",
]
for number, name in enumerate(names, 6):
    try:
        value = version(name)
    except PackageNotFoundError:
        value = "NOT_INSTALLED"
    print(f"V{number:02d} {name}={value}")
PY
```

对应关系固定如下：

| 编号 | 内容 |
|---|---|
| V05 | Python |
| V06 | torch |
| V07 | torch-npu |
| V08 | vllm |
| V09 | vllm-ascend |
| V10 | lmcache |
| V11 | lmcache-ascend |

**版本里的 `.post4`、`rc`、`+……` 等后缀尽量完整保留。**

由于你已经装过补丁，**包版本号还不够，我们还要知道源码提交或修改状态**。把附件中的 `collect_versions.py` 放入服务器后，执行：

```bash
python3 collect_versions.py
```

它会尽量输出这样的短信息：

```text
V10 lmcache=版本号 git=12位提交号 dirty=0
V11 lmcache-ascend=版本号 git=12位提交号 dirty=1
```

这里：

**`git=NA` 只表示没有从安装元数据或可识别源码树里取得提交号，不代表没装补丁；`dirty=0` 也只表示识别到的源码树没有已跟踪文件改动，不是完整的软件指纹证明。**

源码位置等辅助信息只保存在服务器本地的 `logs/inventory_local.txt`，不需要发给我。

## 4. 查当前使用的 CANN

先看当前环境指向哪里：

```bash
printf '%s\n' "${ASCEND_HOME_PATH:-UNSET}"
```

对于带有版本查询脚本的安装结构，执行实际安装目录中的：

```bash
bash "$ASCEND_HOME_PATH/query_pkg_version.sh"
```

如果这个位置没有脚本，应使用实际的 `cann-版本` 安装目录，例如：

```bash
bash /usr/local/Ascend/cann-实际版本/query_pkg_version.sh
```

官方 CANN 9.1.0 安装说明提供了通过 `query_pkg_version.sh` 查询软件包版本的方法。:chatgpt-content-reference{index="2"}

旧式 toolkit 目录也可以查询：

```bash
grep -h '^version=' /usr/local/Ascend/ascend-toolkit/latest/*-linux/ascend_toolkit_install.info
```

这是旧安装结构中官方使用的版本信息文件。:chatgpt-content-reference{index="3"}

记为：

```text
V04 CANN=……
```

**查不到就写 `UNKNOWN`；发现多份而不知道实际使用哪一份，就写 `MULTI`。不要直接取最高版本，也不要把目录里的某份安装当成正在使用的运行时。**

## 5. 把“auto”具体指什么记录下来

这里很重要，因为有几个不同的 `auto`：

| 项目 | 含义 |
|---|---|
| `LMCACHE_MP_TRANSFER_MODE=auto` | 环境变量形式的传输模式选择 |
| `lmcache.mp.mp_transfer_mode=auto` | Connector 配置中的传输模式选择 |
| Server 的 `--supported-transfer-mode=auto` | 服务端允许加载两种传输路径 |
| vLLM 的其他 `auto` 配置 | 可能是执行后端等其他设置，不等于上述传输模式 |

**按当前 LMCache 上游文档，Worker 的传输模式为 `auto` 时，CUDA 走 `lmcache_driven`，其他设备走 `engine_driven`；Connector 中显式设置的值还会覆盖环境变量。** 你使用了补丁，所以最终仍要核对实际安装代码和运行日志，不能直接套用上游默认行为。:chatgpt-content-reference{index="4"}

先查当前 Shell：

```bash
printf 'ENV_MODE=%s\n' "${LMCACHE_MP_TRANSFER_MODE:-UNSET}"
printf 'ALLOC=%s\n' "${PYTORCH_NPU_ALLOC_CONF:-UNSET}"
```

记为：

```text
V15 ENV_MODE=…… CONFIG_MODE=…… SERVER_MODE=…… ACTUAL_MODE=……
V16 ALLOC=……
```

其中 `ACTUAL_MODE` 只在日志明确显示实际选择的路径时填写；不知道就写 `UNKNOWN`。

**当前 Shell 的环境变量，不等于运行中服务的最终配置。** 不需要为了查询模式，把完整启动命令或环境变量全集发出来。

---

# 第二部分：从 `lmcache_driven` 的需求倒推版本

这部分我们不再把所有内容压成一个“IPC 是否支持”的判断，而是建立下面这张对应表。

| 实际需求 | 涉及组件 | 版本倒查重点 | 对应用例 |
|---|---|---|---|
| **跨进程导入并访问 NPU 内存** | TorchNPU → CANN → Driver/Firmware | 实际使用哪组共享接口、哪种分配方式，以及对应支持条件 | T01、T02 |
| **多次导入及资源生命周期** | TorchNPU 分配器、CANN、HDK | 是否包含多次导入相关修复，生产者与导入对象是否正确存活 | T03 |
| **共享 Tensor 视图正确恢复** | TorchNPU、LMCache IPC wrapper | shape、stride、storage offset 的处理是否匹配实际 KV 布局 | T04、T07 |
| **跨进程设备事件** | TorchNPU Event、CANN/HDK、LMCache Event 后端 | 不只看类名存在，还要能导出、导入、等待、查询和同步 | T05、T08 |
| **LMCache 使用这些能力的入口** | LMCache、Ascend 补丁 | 实际注册的 wrapper、Event 后端、CacheContext 是否齐备 | S01、T07、T08 |
| **实际 KV Cache 保存与回填** | CacheContext、Ascend 搬运实现、模型布局 | 正确的布局、数据类型、块编号与执行顺序 | T09 |
| **四个 Worker 与 Server 配合** | 上述组件＋部署配置 | 设备映射、并发注册、生命周期和实际传输路径 | T10、T11 |

内存共享的底层接口、LMCache 的共享封装、缓存上下文和事件后端是不同层次的要求；官方 LMCache 设备适配文档也分别定义了这些接口。:chatgpt-content-reference{index="5"}

## 当前已经能确定的版本约束

**内存 IPC 这一项：** 所查 TorchNPU 特性指南列出的使用基线仍是 **HDK `25.3.RC1` 及以上、CANN `8.3.RC1` 及以上**。相关官方技术说明将 HDK 这一门槛与多次导入修复联系起来。它是这条框架方案的条件，不是所有底层 IPC 的首次出现版本。:chatgpt-content-reference{index="6"}

**Event IPC 这一项：** 不能直接套用内存 IPC 的最低版本。我核对的 TorchNPU `v2.10.0` 源码已经包含 `Event(interprocess=True)`、`ipc_handle()` 和 `from_ipc_handle()`；但这既不是“首次支持版本”的证明，也不是你当前软件栈已经能运行的证明。所以这一项需要 **V07＋实际 Event 用例** 来继续判断。:chatgpt-content-reference{index="7"}

**LMCache 适配这一项：** 要核对实际 commit 和补丁是否注册了需要的接口，不能只看 `pip` 显示“已安装”。当前上游为 wrapper、CacheContext 和 Event 后端分别设置了能力入口。:chatgpt-content-reference{index="8"}

**框架整体配套这一项：** 例如 vLLM-Ascend `v0.23.0` 的安装文档列出 `torch 2.10.0`、`torch-npu 2.10.0.post4`、`CANN 9.1.0`，HDK 则要求继续对照 CANN 配套说明。这个例子说明最终应选的是**版本组合**，不是单独升级一个组件。:chatgpt-content-reference{index="9"}

**本轮不执行任何升级。先拿到你的 V 编号信息，再把“已满足、缺失、未知”填进上表。**

---

# 第三部分：按编号运行简单用例

## 1. 执行方式

把检查包里的文件按你们允许的方式保存到服务器，并保持在同一目录中。它们是明文源码，也可以打开后复制内容保存，不需要把服务器日志传出来。

进入该目录，指定服务使用的 Python：

```bash
export IPC_TEST_PYTHON="$(command -v python3)"
```

以后每个用例只执行一行：

```bash
bash run_probe.sh 用例编号 逻辑卡号
```

例如：

```bash
bash run_probe.sh T00 0
```

**最后的 `0` 是容器内 TorchNPU 的逻辑卡号，不一定等于宿主机物理编号。** 本轮先选一张空闲卡；不要为了测试重新设置可见卡映射。

测试 Tensor 很小，但初始化 NPU 运行时仍会占用额外设备资源，建议在空闲卡或维护窗口执行。

多进程用例使用 `spawn`，因此必须保存为 `.py` 文件运行，不能把整个测试直接作为 `python3 -` 的标准输入执行；子进程需要重新导入主模块。前面的版本查询没有这个限制。:chatgpt-content-reference{index="10"}

## 2. 本轮已经准备好的用例

| 编号 | 具体测试内容 | `PASS` 能说明什么 |
|---|---|---|
| **T00** | 单进程在 NPU 上分配 Tensor、加法、求和 | 当前 Python/TorchNPU 能完成基础设备运算 |
| **S01** | 检查当前 LMCache 后端的 wrapper、Event 后端、CacheContext 入口 | **仅说明相应接口已声明或绑定，不说明功能已经通过** |
| **T01** | 使用当前分配器配置，进程 A 分配，B 读取并修改，A 检查修改 | 不只是“两边数值相同”，而是验证共享写入可见 |
| **T02** | 显式启用 `expandable_segments:True`，重复跨进程读写 | 这一分配器配置下的 TorchNPU 共享路径通过 |
| **T03** | 让三个新消费者进程依次导入同一存储，并逐次修改 | 小规模重复跨进程导入通过，避免只重复读取同一个已缓存对象 |
| **T04** | 共享带偏移和非连续 stride 的 Tensor 视图 | 合成视图的元数据及原始存储修改范围正确 |
| **T05** | TorchNPU 原生 Event 的跨进程导出、导入、wait/query/sync | 原生 Event IPC 的基础 API 闭环通过 |
| **T06** | Torch pinned CPU 内存与 NPU 之间 H2D、D2H 搬运 | 这一辅助搬运路径通过 |
| **T07** | 调用当前 LMCache 注册的真实 IPC wrapper，跨进程序列化、恢复和回写 | 不再只测裸 Tensor，而是测安装环境里的 LMCache wrapper |
| **T08** | 调用当前 LMCache 注册的真实 Event 后端，跨进程执行事件操作 | 安装环境里的 LMCache Event 后端基础闭环通过 |

T07 使用的 wrapper 契约包括包装、序列化和 `to_tensor()` 恢复；T08 使用的是 LMCache 定义的 Event 后端契约，而不是拿 Python `multiprocessing.Event` 替代设备事件。:chatgpt-content-reference{index="11"}

### 有四个边界需要保留

**T02 仍然经过 TorchNPU，不是绕过框架的“原生 CANN 实验”。**

**T05/T08 是 Event 基础闭环检查，不是未完成任务、竞态压力或所有异步顺序场景的证明。** 真正的缓存传输顺序还需要放进 T09 验证。

**T06 是辅助项。** 当前 LMCache 文档没有把统一的 host-pinning 后端列为启用 `lmcache_driven` 的通用硬门槛；你的插件也可能使用自己的分配方式。因此，T06 失败不能单独否定整个模式。:chatgpt-content-reference{index="12"}

**这些用例使用小型合成 Tensor，不等于已经覆盖实际模型的 KV 数据类型、布局或 TP=4。**

## 3. S01 的结果怎样看？

执行：

```bash
bash run_probe.sh S01 0
```

输出中可能包含：

```text
CAP W1 E0 C1 H1
```

含义是：

| 标记 | 含义 |
|---|---|
| W | IPC wrapper 是否绑定 |
| E | Event IPC 后端是否绑定 |
| C | CacheContext 创建入口是否覆写了基类默认实现 |
| H | 后端是否声明允许句柄传输 |

例如 `W1 E0 C1 H1` 表示：wrapper 和缓存上下文入口存在，但 Event 后端没有绑定。

**即使四个值全部为 1，也只是接口检查通过，不能直接当成服务全链路通过。**

S01、T07、T08 按本次核对的 `DeviceSpec` 接口编写。你的旧补丁或 fork 如果使用另一种接口组织方式，可能输出：

```text
S01 SKIP API_PROFILE
```

这表示**探针需要按你实际 commit 调整**，不是“910B3 不支持”。

## 4. 第一轮不用把所有用例都跑一遍

先执行：

```bash
bash run_probe.sh T00 0
```

**T00 失败，就先停下设备测试，回报版本和这一项结果。**

T00 成功后，再分别执行：

```bash
bash run_probe.sh S01 0
```

```bash
bash run_probe.sh T01 0
```

```bash
bash run_probe.sh T05 0
```

这几项分别回答：基础运算是否正常、插件能力是否绑定、内存 IPC 是否工作、设备 Event IPC 是否工作。

**T01 失败，或者确实需要做分配器对照时，再执行：**

```bash
bash run_probe.sh T02 0
```

如果 V16 显示当前本来就是相同的 `expandable_segments:True` 配置，就不用重复执行，记为：

```text
T02 SKIP SAME_CONFIG
```

随后按已有能力继续：

| 条件 | 下一项 |
|---|---|
| T02 通过，需要检查重复导入与视图 | T03、T04 |
| S01 中 W=1 | T07 |
| S01 中 E=1 | T08 |
| 需要单独检查 CPU/NPU 搬运 | T06 |

**T02/T03/T04 的分配器设置只作用于测试子进程，不会修改已经运行的服务。** T07/T08 默认继承原环境；如果之后要在另一种分配器配置下重测，结果要另标为 `T07(VMM)`，不能与原来的结果混在一起。

## 5. 报错不用抄长日志

脚本会把完整输出留在：

```text
logs/T02_d0.log
```

终端只显示类似：

```text
T00 PASS d=0
```

或者：

```text
T02 FAIL d=0 rc=1 RECV_IMPORT:RuntimeError drvRetCode=17
```

这些只是格式示例，不是实际测试结果。

**你最少只抄：**

```text
T00 PASS
T02 FAIL
```

失败时能够多抄一点，则用：

```text
T02 FAIL RECV_IMPORT 17/507899
```

不用抄路径、堆栈和重复告警。

`SKIP` 表示这项没有完成或测试适配条件不满足；**不算通过，也不算已经证明硬件不支持**。

## 6. 剩下的实际需求先固定编号，等版本和补丁确认后补对应命令

这几项必须依照你的真实插件接口、模型布局和部署方式编写，本轮不猜参数：

| 编号 | 要验证的内容 |
|---|---|
| **D01** | 当 Tensor IPC 失败时，用匹配当前 CANN 版本的原生接口完成 export/import/map/读写闭环，区分框架封装与底层接口 |
| **T09** | 使用实际 LMCache CacheContext 和搬运实现：小批 KV 执行 **STORE → 清空 → RETRIEVE → 内容校验**，同时检查未选中区域和事件顺序 |
| **T10** | 四个 Worker 在各自设备分配，由接收端在对应设备上下文导入，再测试小规模并发注册和释放 |
| **T11** | 独立测试服务显式启用 `lmcache_driven`：TP=4 初始化、真实缓存保存和回填，并确认命中的是 LMCache，而非只命中 vLLM 自身缓存 |

**这四项暂未放入可执行脚本，当前应记 `NOT_RUN`。** 特别是 T09，只有注册入口存在还不够，需要真实构造 CacheContext 并完成搬运；官方基类也把上下文、指针和缓冲区管理作为独立接口。:chatgpt-content-reference{index="13"}

---

# 第一轮你回报这些就够了

可以按下面格式手工输入，不必一次填满：

```text
HW=910B3
环境=同一容器 / Worker和Server分开

V01 Driver=
V02 Firmware=
V03 Compatibility=
V04 CANN=
V05 Python=
V06 torch=
V07 torch-npu=
V08 vllm=
V09 vllm-ascend=
V10 lmcache=  git=
V11 lmcache-ascend=  git=

V15 ENV_MODE=  CONFIG_MODE=  SERVER_MODE=  ACTUAL_MODE=
V16 ALLOC=

T00=
S01=  W= E= C= H=
T01=
T05=
T02=未运行 / PASS / FAIL / SKIP
```

**第一轮重点就是“版本＋S01＋T00/T01/T05”，必要时加 T02。** 这能先区分问题更接近内存共享、设备事件、插件绑定，还是运行环境不一致；暂时不需要反复启动大模型，也不需要你手工搬运大段错误日志。